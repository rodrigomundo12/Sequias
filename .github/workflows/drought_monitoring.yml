name: Drought Monitoring - Local Windows

on:
  workflow_dispatch:

jobs:

  test-local-runner:

    runs-on: self-hosted

    steps:

      - name: Test local runner
        shell: cmd
        run: |
          echo ==============================================
          echo SEQUIAS LOCAL RUNNER TEST
          echo ==============================================

          echo.
          echo Computer:
          echo %COMPUTERNAME%

          echo.
          echo User:
          echo %USERNAME%

          echo.
          echo Current directory:
          cd

          echo.
          echo Python:
          python --version

          echo.
          echo Python executable:
          where python

          echo.
          echo Project directory:
          if exist "C:\Users\LLRL-PAHs2\Downloads\sequias-poligonos" (
              echo TRUE
          ) else (
              echo FALSE
          )

          echo.
          echo ==============================================
          echo RUNNER TEST COMPLETE
          echo ==============================================